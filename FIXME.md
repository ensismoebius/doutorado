# For software/efficient_nn_lab

- At backpropagation section add one more screen demonstrating how, in principle, neural networks are just vectors and matrices multiplication, this screen must make the visual mapping between a neural network graph with its weighs, inputs, outputs and activation function and error function with its respective vectors and matrices. The animation must show with numeric values how the forward and backwards work across these vector and matrices.

- And must show the chain rule applied to this network.