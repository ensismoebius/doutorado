% TikZ figure: Linear-band energies computation pipeline
\begin{tikzpicture}[
    >=stealth,
    thick,
    every node/.style={font=\small},
    mbox/.style={
        draw=blue!80!black, thick,
        inner sep=7pt, align=center,
        minimum height=0.8cm, minimum width=7.8cm
    },
]
\colorlet{bb}{blue!80!black}

\def\xL{-2.8}
\def\xM{0}
\def\xR{2.8}
\def\xLbl{4.1}

% ── Ellipse ───────────────────────────────────────────────────────────────────
\node[draw=bb, thick, ellipse, minimum width=4cm, minimum height=0.9cm]
    (sig) at (0,0) {Sinal de voz};

% ── Branch lines with linearly-spaced freq labels ─────────────────────────────
\coordinate (bL) at (\xL,-1.8);
\coordinate (bM) at (\xM,-1.8);
\coordinate (bR) at (\xR,-1.8);

\draw (sig.210) -- (bL)
    node[pos=0.45, left,  font=\footnotesize] {300--400\,Hz};
\draw (sig.270) -- (bM)
    node[pos=0.45, font=\footnotesize] {1750--1850\,Hz};
\draw (sig.330) -- (bR)
    node[pos=0.45, right, font=\footnotesize] {3300--3400\,Hz};

% ── Energy sum row ────────────────────────────────────────────────────────────
\node (sumL) at (\xL,-2.6) {$\textstyle\sum_i(y_i)^2$};
\node (sumM) at (\xM,-2.6) {$\textstyle\sum_i(y_i)^2$};
\node        at ({0.5*\xM+0.5*\xR},-2.6) {$\cdots\!\cdots$};
\node (sumR) at (\xR,-2.6) {$\textstyle\sum_i(y_i)^2$};
\node[right] at (\xLbl,-2.6) {Etapa de soma};

\draw (bL) -- (sumL.north);
\draw (bM) -- (sumM.north);
\draw (bR) -- (sumR.north);

% ── Feature vector box ────────────────────────────────────────────────────────
\node[mbox] (vbox) at (0,-4.2)
    {$V_1\quad V_2\quad\cdots\!\cdots\!\cdots\quad V_N$};
\node[right, align=left] at (\xLbl,-4.2) {Vetor de\\características};

\draw[->] (sumL.south) -- (sumL |- vbox.north);
\draw[->] (sumM.south) -- (sumM |- vbox.north);
\draw[->] (sumR.south) -- (sumR |- vbox.north);

\end{tikzpicture}