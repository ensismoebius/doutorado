% TikZ figure: BFCC feature vector computation pipeline
\begin{tikzpicture}[
    >=stealth,
    thick,
    every node/.style={font=\small},
    mbox/.style={
        draw=diagramOutlineColor, thick,
        inner sep=7pt, align=center,
        minimum height=0.8cm, minimum width=7.8cm
    },
]
\colorlet{bb}{diagramOutlineColor}

\def\xL{-2.8}
\def\xM{0}
\def\xR{2.8}
\def\xLbl{4.1}

% ── Ellipse ───────────────────────────────────────────────────────────────────
\node[draw=bb, thick, ellipse, minimum width=4cm, minimum height=0.9cm]
    (sig) at (0,0) {Sinal de voz};

% ── Branch lines with Bark-scale freq labels ──────────────────────────────────
\coordinate (bL) at (\xL,-1.8);
\coordinate (bM) at (\xM,-1.8);
\coordinate (bR) at (\xR,-1.8);

\draw (sig.210) -- (bL)
    node[pos=0.45, left,  font=\footnotesize] {20--100\,Hz};
\draw (sig.270) -- (bM)
    node[pos=0.45, font=\footnotesize] {101--200\,Hz};
\draw (sig.330) -- (bR)
    node[pos=0.45, right, font=\footnotesize] {12001--15500\,Hz};

% ── Energy sum row ────────────────────────────────────────────────────────────
\node (sumL) at (\xL,-2.6) {$\textstyle\sum_i(y_i)^2$};
\node (sumM) at (\xM,-2.6) {$\textstyle\sum_i(y_i)^2$};
\node        at ({0.5*\xM+0.5*\xR},-2.6) {$\cdots\!\cdots$};
\node (sumR) at (\xR,-2.6) {$\textstyle\sum_i(y_i)^2$};
\node[right] at (\xLbl,-2.6) {Etapa de soma};

\draw (bL) -- (sumL.north);
\draw (bM) -- (sumM.north);
\draw (bR) -- (sumR.north);

% ── Log box ───────────────────────────────────────────────────────────────────
\node[mbox] (logbox) at (0,-4.3)
    {$\log(V_1)\quad\log(V_2)\quad\cdots\!\cdots\!\cdots\quad\log(V_{24})$};

\draw[->] (sumL.south) -- (sumL |- logbox.north);
\draw[->] (sumM.south) -- (sumM |- logbox.north);
\draw[->] (sumR.south) -- (sumR |- logbox.north);

% ── Feature vector box (log energies) ─────────────────────────────────────────
\node[mbox] (vbox1) at (0,-5.9)
    {$V_1\quad V_2\quad\cdots\!\cdots\!\cdots\quad V_{24}$};
\node[right, align=left] at (\xLbl,-5.9) {Vetor de\\características};
\draw[->] (logbox.south) -- (vbox1.north);

% ── DCT box ───────────────────────────────────────────────────────────────────
\node[mbox, minimum height=1.0cm] (dctbox) at (0,-7.4)
    {Aplicação da DCT};
\node[right, font=\footnotesize, align=left] at (\xLbl,-7.4)
    {DCT -- \textit{Discrete cosine transformation}};
\draw[->] (vbox1.south) -- (dctbox.north);

% ── BFCC coefficient vector ───────────────────────────────────────────────────
\node[mbox] (vbox2) at (0,-8.9)
    {$V_1\quad V_2\quad\cdots\!\cdots\!\cdots\quad V_{24}$};
\node[right, align=left] at (\xLbl,-8.9) {Coeficientes\\BFCC};
\draw[->] (dctbox.south) -- (vbox2.north);

\end{tikzpicture}