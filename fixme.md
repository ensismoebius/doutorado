# fixme.md — moved to the wiki (2026-07-19)

This file's content — the D1–D6 decision log, the network-parity bugs found against
PyTorch/snnTorch, the optimizer SOTA work, the untrustworthy-comment audit, and the
pending/deferred items — has been merged into the permanent wiki:

- **[Engineering Fixes Log](software/nn/.wiki/Guides/Engineering-Fixes-Log.md)** — the full log,
  reorganized and cross-linked. Source comments citing `fixme.md D3` (etc.) now point here.
- **[Re-run Runbook](software/nn/.wiki/Guides/Re-run-Runbook.md)** — the re-run commands, moved
  from `run.md`.

Open items (item 51's optimizer ablation, the `weight_decay`/`firing_rate_reg_lambda`/tdBN
end-to-end exercise) are tracked in the Engineering Fixes Log's "Pending / open items" section,
not here.
