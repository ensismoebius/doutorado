numpy>=1.25
scipy>=1.11
tqdm

# Live experiment-monitor dashboard (scripts/pipeline/meeting01/monitor.py).
# Pure-Python. The monitor's --plain and --self-test modes need only the stdlib.
rich>=13.0

# Multi-panel GridUnesp control TUI (scripts/pipeline/meeting01/gridunesp_tui.py) --
# LOCAL machine only, never needed on GridUnesp itself: the remote-side status
# script it drives (gridunesp_status_remote.py) is stdlib + monitor.py's own
# ingestion classes only, no textual/rich dependency, so it runs fine on a bare
# GridUnesp Python with no venv at all.
textual>=0.80

# Web dashboard backend (scripts/pipeline/meeting01/dashboard/server.py) --
# LOCAL machine only, never needed on GridUnesp itself.
fastapi
uvicorn

# CPU-only PyTorch wheels — this project trains on CPU, not CUDA, and the
# CUDA build pulls several GB of unused nvidia-* packages. Needed by
# scripts/data/npz_to_pytorch.py and the
# scripts/testing/gen_*_refs.py reference generators.
--extra-index-url https://download.pytorch.org/whl/cpu
torch
snntorch
lion-pytorch
schedulefree
muon-optimizer
# Ground truth for ThresholdDependentBatchNorm (tdBN) in gen_pytorch_refs.py --
# spikingjelly.activation_based.layer.ThresholdDependentBatchNorm1d implements the same
# Zheng et al. AAAI 2021 formula our ThresholdDependentBatchNorm.hpp does.
spikingjelly
