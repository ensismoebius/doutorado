# Core autoencoder models - headers only library
# These headers provide the interface for ANN/SNN autoencoders
# Implementations are in experiments for now

# Headers are in the same directory
# No build targets needed for header-only interface

add_subdirectory(tests)