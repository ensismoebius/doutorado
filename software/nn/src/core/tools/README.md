# tools

Purpose
- Small tools and benchmarks useful for development (e.g., `prefetch_benchmark`, demo utilities).

Usage
- Build the relevant executable targets and run from the build directory. Examples are present in `src/core/tools/` sources.

Tests
- Tools typically have ad-hoc test code or are exercised manually during development.
