# linearAlgebra

Purpose
- Linear algebra helpers and bridges between tensor abstractions and low-level numerical routines.

Usage
- Provide utility functions used by the `tensor` module and layers. Prefer the high-level `nn::Tensor` API when possible.

Tests
- Unit tests illustrating numeric behavior live under `src/core/linearAlgebra/tests/`.
