# paraconsistent

Purpose
- Implementation of paraconsistent logic utilities used by experimentation code (project-specific analysis routines).

Usage
- Refer to the functions in this folder from experiment code in `src/experiments/` when paraconsistent metrics are needed.

Tests
- See `src/core/paraconsistent/tests/` for example usage.
