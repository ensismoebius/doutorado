{
    "_comment": "Nested format - testing",
    "experiment": {
        "run_tag": "debug",
        "seed": 42,
        "repeats": 1,
        "seed_deterministic": false,
        "check_determinism": false
    },
    "dataset": {
        "dataset_root": "/home/ensismoebius/Documentos/academico/UNESP/doutorado/databases/fsdDataset",
        "results_dir": "results/meeting01",
        "window_size": 64,
        "max_loaded_train_samples": 20,
        "max_validation_samples": 10,
        "_loso_caps_comment": "max_loaded_train_samples / max_validation_samples are IGNORED once cv_fold >= 0 (see Meeting01Config.cpp::check_dataset). Without the loso_max_* caps below, running this profile with --cv-fold 0 silently trains on every window of FSDD -- ~105k batches per epoch, hours per run, on a profile named 'debug'.",
        "loso_max_train_windows": 20,
        "loso_max_val_windows": 10,
        "loso_max_test_windows": 20
    },
    "training": {
        "samples_per_batch": 1,
        "batches_per_epoch": 0,
        "epochs": 2,
        "early_stop_patience": 1,
        "learning_rate": 0.001,
        "max_reconstruct_mean_deviation": 0.25
    },
    "model": {
        "encoder_layer_spec": [
            "linear:16:leaky",
            "linear:8:identity"
        ],
        "decoder_layer_spec": [
            "linear:8:leaky",
            "linear:output:identity"
        ],
        "time_steps": 4
    },
    "evaluation": {
        "datasets": [
            "fsdd"
        ],
        "encodings": [
            "direct"
        ],
        "snn_architectures": [
            "dense"
        ],
        "ga": {
            "snn": {
                "population_size": 1,
                "generations": 0,
                "winner_seeds": 1,
                "voltage_threshold_min": 1.0,
                "voltage_threshold_max": 1.0,
                "alpha_min": 0.9,
                "alpha_max": 0.9
            }
        }
    }
}
