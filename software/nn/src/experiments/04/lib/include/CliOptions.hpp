#pragma once

#include <string>

namespace comparative_autoencoder_experiment
{

struct CliOptions
{
    std::string comparative_config;
    bool help = false;
};

} // namespace comparative_autoencoder_experiment
