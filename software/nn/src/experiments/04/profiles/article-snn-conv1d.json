{
    "_profile_name": "article-snn-conv1d",
    "_network_type": "SNN autoencoder, 3-tap temporal smoothing input pre-process",
    "_architecture_summary": "Same network as article-snn-dense (Linear+Lif x2 encoder, Linear+Lif -> Linear+LifIntegrator decoder). Mode 'conv1d' applies a fixed {0.25, 0.5, 0.25} 3-tap symmetric temporal smoothing to the encoded input BEFORE the SNN. Not a learned Conv1D layer.",
    "_loss_function": "MSE reconstruction loss",
    "_preprocessing": "Non-overlapping windows of 256 samples (32 ms @ 8 kHz). Z-score per window. Encoding then 3-tap smoothing (mode='conv1d').",
    "_total_runs_breakdown": "81 = 3 encodings x 3 V_th x 3 alpha x 3 repeats.",
    "_dataset_config": "100 train, 30 val from FSDD (shuffled split). 30 epochs max, early stop patience 5, lr 0.001 (weights), 0.0001 (R,C,V_th), batch size 1.",
    "_expected_outputs": "results/article_snn_conv1d_*.csv; .../data/article_snn_conv1d_summary.dat, _sweep.dat.",
    "_comment": "SNN-AE with 3-tap temporal smoothing pre-filter. 81 runs. ~45 min.",
    "experiment": {
        "run_tag": "article_snn_conv1d",
        "seed": 42,
        "repeats": 3,
        "seed_deterministic": false,
        "check_determinism": false
    },
    "dataset": {
        "dataset_root": "/home/ensismoebius/Documentos/academico/UNESP/doutorado/databases/fsdDataset",
        "results_dir": "results",
        "window_size": 256,
        "max_loaded_train_samples": 100,
        "max_validation_samples": 30,
        "latex_data_dir": "/home/ensismoebius/Repos/doutorado/documentation/07-articlesProduced/conference71070Guaiaquil/data",
        "save_models": true
    },
    "training": {
        "samples_per_batch": 1,
        "batches_per_epoch": 0,
        "epochs": 30,
        "early_stop_patience": 5,
        "learning_rate": 0.001,
        "learning_rate_biophysical": 0.0001,
        "max_reconstruct_mean_deviation": 0.25,
        "beta1": 0.9,
        "beta2": 0.999,
        "epsilon": 1e-8
    },
    "model": {
        "loss_function": "mse",
        "latent_dim": 32,
        "lstm_hidden_size": 64,
        "encoder_layer_spec": [
            "linear:64:leaky",
            "linear:32:identity"
        ],
        "decoder_layer_spec": [
            "linear:64:leaky",
            "linear:output:identity"
        ]
    },
    "evaluation": {
        "datasets": [
            "fsdd"
        ],
        "encodings": [
            "direct",
            "poisson",
            "latency"
        ],
        "snn_architectures": [
            "conv1d"
        ],
        "v_th_values": [
            0.5,
            1.0,
            1.5
        ],
        "alpha_values": [
            0.8,
            0.9,
            0.99
        ]
    }
}
