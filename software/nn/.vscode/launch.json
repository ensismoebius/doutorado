{
    "version": "0.2.0",
    "configurations": [
        // ─────────────────────────────────────────────────────────────────
        // Generic: debug whichever target is selected in the CMake toolbar.
        // Select a target in the bottom status bar, then press F5.
        // ─────────────────────────────────────────────────────────────────
        {
            "name": "CMake: active target (lldb-dap)",
            "type": "lldb-dap",
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",
            "args": [],
            "cwd": "${workspaceFolder}",
            "initCommands": [
                "command script import ${workspaceFolder}/debug/lldb/eigenlldb.py"
            ],
            "env": {
                "ASAN_OPTIONS": "detect_leaks=0",
                "LSAN_OPTIONS": "verbosity=1:log_threads=1"
            },
            "preLaunchTask": "CMake: build"
        },
        {
            "name": "CMake: active target (GDB + Eigen printers)",
            "type": "cppdbg",
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            "MIMode": "gdb",
            "miDebuggerPath": "/usr/bin/gdb",
            "setupCommands": [
                {
                    "description": "Enable GDB pretty-printing",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                },
                {
                    "description": "Load Eigen/STL pretty-printers",
                    "text": "python import sys; sys.path.insert(0, '${workspaceFolder}/debug/gdb'); from printers import register_printers; register_printers(None)",
                    "ignoreFailures": true
                }
            ],
            "preLaunchTask": "CMake: build"
        },
        // ─────────────────────────────────────────────────────────────────
        // Experiment 03
        // ─────────────────────────────────────────────────────────────────
        {
            "name": "experiment03 (lldb-dap)",
            "type": "lldb-dap",
            "request": "launch",
            "program": "${command:cmake.buildDirectory}/src/experiments/03/experiment03",
            "args": [],
            "cwd": "${workspaceFolder}",
            "initCommands": [
                "command script import ${workspaceFolder}/debug/lldb/eigenlldb.py"
            ],
            "env": {
                "ASAN_OPTIONS": "detect_leaks=0"
            },
            "preLaunchTask": "CMake: build"
        },
        {
            "name": "experiment03 (GDB + Eigen printers)",
            "type": "cppdbg",
            "request": "launch",
            "program": "${command:cmake.buildDirectory}/src/experiments/03/experiment03",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            "MIMode": "gdb",
            "miDebuggerPath": "/usr/bin/gdb",
            "setupCommands": [
                {
                    "description": "Enable GDB pretty-printing",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                },
                {
                    "description": "Load Eigen/STL pretty-printers",
                    "text": "python import sys; sys.path.insert(0, '${workspaceFolder}/debug/gdb'); from printers import register_printers; register_printers(None)",
                    "ignoreFailures": true
                }
            ],
            "preLaunchTask": "CMake: build"
        },
        // ─────────────────────────────────────────────────────────────────
        // Experiment 02
        // ─────────────────────────────────────────────────────────────────
        {
            "name": "experiment_02 (lldb-dap)",
            "type": "lldb-dap",
            "request": "launch",
            "program": "${command:cmake.buildDirectory}/src/experiments/02/experiment_02",
            "args": [],
            "cwd": "${workspaceFolder}",
            "initCommands": [
                "command script import ${workspaceFolder}/debug/lldb/eigenlldb.py"
            ],
            "preLaunchTask": "CMake: build"
        },
        {
            "name": "experiment_02 (GDB + Eigen printers)",
            "type": "cppdbg",
            "request": "launch",
            "program": "${command:cmake.buildDirectory}/src/experiments/02/experiment_02",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            "MIMode": "gdb",
            "miDebuggerPath": "/usr/bin/gdb",
            "setupCommands": [
                {
                    "description": "Enable GDB pretty-printing",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                },
                {
                    "description": "Load Eigen/STL pretty-printers",
                    "text": "python import sys; sys.path.insert(0, '${workspaceFolder}/debug/gdb'); from printers import register_printers; register_printers(None)",
                    "ignoreFailures": true
                }
            ],
            "preLaunchTask": "CMake: build"
        },
        // ─────────────────────────────────────────────────────────────────
        // Experiment 00 (Phase00)
        // ─────────────────────────────────────────────────────────────────
        {
            "name": "Phase00 (lldb-dap)",
            "type": "lldb-dap",
            "request": "launch",
            "program": "${command:cmake.buildDirectory}/src/experiments/00/Phase00",
            "args": [],
            "cwd": "${workspaceFolder}",
            "initCommands": [
                "command script import ${workspaceFolder}/debug/lldb/eigenlldb.py"
            ],
            "preLaunchTask": "CMake: build"
        },
        // ─────────────────────────────────────────────────────────────────
        // Demos
        // ─────────────────────────────────────────────────────────────────
        {
            "name": "voice_biometrics_cpp (lldb-dap)",
            "type": "lldb-dap",
            "request": "launch",
            "program": "${command:cmake.buildDirectory}/src/demos/voice_biometrics_cpp/voice_biometrics_cpp",
            "args": [],
            "cwd": "${workspaceFolder}",
            "initCommands": [
                "command script import ${workspaceFolder}/debug/lldb/eigenlldb.py"
            ],
            "preLaunchTask": "CMake: build"
        },
        {
            "name": "wavelet_demo (lldb-dap)",
            "type": "lldb-dap",
            "request": "launch",
            "program": "${command:cmake.buildDirectory}/src/demos/wavelet_demo/wavelet_demo",
            "args": [],
            "cwd": "${workspaceFolder}",
            "initCommands": [
                "command script import ${workspaceFolder}/debug/lldb/eigenlldb.py"
            ],
            "preLaunchTask": "CMake: build"
        },
        {
            "name": "exec_lfcc_pipeline (lldb-dap)",
            "type": "lldb-dap",
            "request": "launch",
            "program": "${command:cmake.buildDirectory}/src/demos/lfcc_pipeline/exec_lfcc_pipeline",
            "args": [],
            "cwd": "${workspaceFolder}",
            "initCommands": [
                "command script import ${workspaceFolder}/debug/lldb/eigenlldb.py"
            ],
            "preLaunchTask": "CMake: build"
        }
    ]
}
