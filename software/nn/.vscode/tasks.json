{
	"tasks": [
		{
			"type": "process",
			"label": "compile",
			"command": "/opt/homebrew/bin/g++-11",
			"args": [
				"-g",
				"${file}",
				"-o",
				"${fileDirname}/${fileBasenameNoExtension}",
			]
		}
	],
	"version": "2.0.0"
}