- Replaced `LIKELY` and `UNLIKELY` macros with C++20 `[[likely]]` and `[[unlikely]]` attributes in `src/core/layers/Conv2d.hpp`, `src/core/layers/Conv2d_impl.cpp`, and `src/core/layers/Conv2d_utils.cpp`.
- Applied `[[unlikely]]` to buffer resizing conditions in `src/core/layers/Conv2d_old.hpp`.
- Applied `[[unlikely]]` to `v_mem` resizing conditions and `[[likely]]` to the main execution path of `tau` check in `src/core/layers/Leaky.hpp`.
- Applied `[[unlikely]]` to error/infrequent conditions in `src/core/layers/MaxPool2d.hpp` (invalid input shape) and `src/core/layers/MSELoss.hpp` (non-finite values, non-finite gradients, gradient clipping).
- Applied `[[likely]]` to the common path of shape alignment in `src/core/layers/ResNetBlock.hpp`.